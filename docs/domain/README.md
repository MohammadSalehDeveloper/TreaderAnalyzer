# Domain model docs

Add `docs/domain/{name}.md` when introducing or materially changing an aggregate, entity, value object, or enum. Include invariants, factory methods, and relations.

Current types live in `src/Core/Core.Domain`. Domain changes require architecture-guard approval.
