# Local Docker SQL Server (never use these values in production).
# Copy to .env in this folder if you want to override defaults:
#   copy .env.example .env

MSSQL_SA_PASSWORD=TraderAnalyzer_Dev!23
MSSQL_PORT=14333
